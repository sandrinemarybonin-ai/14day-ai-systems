def main():
    prompt = "Explain Input → Processing → Model → Output → Feedback in one sentence."
    # Placeholder for Day 3: we’ll replace this with a real model call
    response = (
        "An AI system takes input, processes it into a usable form, "
        "uses a model to produce an output, and uses feedback to improve future results."
    )

    print("PROMPT:")
    print(prompt)
    print("\nRESPONSE:")
    print(response)

if __name__ == "__main__":
    main()